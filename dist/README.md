# python-data-analysis

실무에서 하나의 컬럼이 날아가면 아주 민감한 만큼 delete, drop은 안쓰는 게 좋음.

⭐ 파일이 여러개 일때는 먼저 취합하고, 그래프 시각화 하기(중요!!)

ctrl + shift + c키를 누르고, 마우스 갇다대면 코드를 쉽게 찾을 수 있음(F12키 누른 후 Elements 코드에서 찾을 수 있음)

localhost 주소마다 다르므로 없으면 설치하기!!

selenium은 venv 가상환경이 없으면 안됨(모르면 제미나이, 클로드에게 물어보기).
